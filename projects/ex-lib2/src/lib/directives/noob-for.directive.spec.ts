import { NoobForDirective } from './noob-for.directive';

describe('NoobForDirective', () => {
  it('should create an instance', () => {
  /*   const directive = new NoobForDirective();
    expect(directive).toBeTruthy(); */
  });
});
